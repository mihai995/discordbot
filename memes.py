#!/usr/bin/python3.5

import asyncio

async def postmemes(bot, msg):
  await self.reply(msg, 'functionality not yet implemented')

async def approve(bot, msg):
  await self.reply(msg, 'functionality not yet implemented')
